namespace MarsRover.Enums
{
    /// <summary>
    /// Represents a command for a Rover.
    /// </summary>
    public enum Command
    {
        TurnLeft = 0,
        TurnRight = 1,
        MoveForward = 2
    }
}
